---
// src/integrations/chatbot/chatbot.integration.astro
// UI-only mode: the KB generator is disabled, so we no longer gate on
// KNOWLEDGE_BASE. The widget renders purely for its UI; its send handler returns
// a canned reply and makes NO /api/chat call.
import ChatBot from "./ChatBot.tsx";
---

<ChatBot client:firstInteraction />
